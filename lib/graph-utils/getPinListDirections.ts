import type { BpcPin, Direction } from "lib/types"

/**
 * Computes the bound sand edges that the pinlist is on, then computes a
 * direction for each pin.
 */
export const getPinListDirections = (pinList: BpcPin[]): Direction[] => {}
