import type { BpcGraph, FixedBpcGraph } from "lib/types"
import { useEffect, useState } from "react"

export const Adapt2Debugger = ({
  floatingGraph,
  fixedGraph,
}: {
  floatingGraph: BpcGraph
  fixedGraph: FixedBpcGraph
}) => {}

export default () => {}
